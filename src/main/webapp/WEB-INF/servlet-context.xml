<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:context="http://www.springframework.org/schema/context"
             xmlns:mvc="http://www.springframework.org/schema/mvc"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xsi:schemaLocation="
                http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
                http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- @Controller 어노테이션 활성화 + MessageConverter 등 자동 설정 -->
    <mvc:annotation-driven />

    <!-- 정적 자원(css/js/img 등)을 /resources/** 경로로 매핑 -->
    <mvc:resources mapping="/resources/**" location="/resources/" />

    <!-- @Component, @Service, @Repository, @Controller 스캔 -->
    <context:component-scan base-package="com.spring.ion" />

    <!-- 뷰 리졸버 설정: /WEB-INF/views/ 폴더에서 .jsp 파일을 찾아 렌더링 -->
    <beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <beans:property name="prefix" value="/WEB-INF/views/" />
        <beans:property name="suffix" value=".jsp" />
    </beans:bean>
</beans:beans>
