<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MemberAuthority">

    <update id="promoteToAuthMember" parameterType="map">
        UPDATE member_authority
        SET authority = 'ROLE_AUTH_MEMBER'
        WHERE userId = #{userId} AND authority = 'ROLE_MEMBER'
    </update>

    <insert id="insertRoleAuthMember" parameterType="map">
        INSERT IGNORE INTO member_authority (userId, authority)
        VALUES (#{userId}, 'ROLE_AUTH_MEMBER')
    </insert>

    <delete id="deleteRoleMember" parameterType="map">
        DELETE FROM member_authority
        WHERE userId = #{userId} AND authority = 'ROLE_MEMBER'
    </delete>
</mapper>

