<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Report">
    <resultMap id="ReportMap" type="Report">
        <id     column="id"               property="id"/>
        <result column="reporterId"       property="reporterId"/>
        <result column="targetBoard"      property="targetBoard"/>
        <result column="targetContentId"  property="targetContentId"/>
        <result column="type"             property="type"/>
        <result column="status"           property="status"/>
        <result column="description"      property="description"/>
    </resultMap>

    <!-- mapper/yjw/reportMapper.xml -->
    <insert id="insertReport" parameterType="Report" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO report (
        reporterId, targetBoard, targetContentId, type, status, description
        ) VALUES (
        #{reporterId}, #{targetBoard}, #{targetContentId}, #{type},
        <choose>
            <when test="status != null">#{status}</when>
            <otherwise>DEFAULT</otherwise>
        </choose>,
        #{description}
        )
    </insert>

    <!-- 단건 조회 -->
    <select id="findById" parameterType="long" resultMap="ReportMap">
        SELECT
        id, reporterId, targetBoard, targetContentId, `type`, `status`, `description`
        FROM report
        WHERE id = #{id}
    </select>

    <!-- 전체(최신순) -->
    <select id="findAllOrderByNewest" resultMap="ReportMap">
        SELECT id, reporterId, targetBoard, targetContentId, `type`, `status`, `description`
        FROM report
        ORDER BY id DESC
    </select>

    <!-- 게시판별 -->
    <select id="findByBoard" parameterType="string" resultMap="ReportMap">
        SELECT id, reporterId, targetBoard, targetContentId, `type`, `status`, `description`
        FROM report
        WHERE `targetBoard` = #{board}
        ORDER BY id DESC
    </select>

    <!-- 미처리 목록 -->
    <select id="findPending" resultMap="ReportMap">
        SELECT id, reporterId, targetBoard, targetContentId, `type`, `status`, `description`
        FROM report
        WHERE `status` = 'PENDING'
        ORDER BY id DESC
    </select>

    <!-- 상태 변경 -->
    <update id="updateStatus" parameterType="map">
        UPDATE report
        SET `status` = #{status}
        WHERE id = #{id}
    </update>

    <!-- 삭제 -->
    <delete id="delete" parameterType="long">
        DELETE FROM report
        WHERE id = #{id}
    </delete>

    <select id="findPendingByBoard" parameterType="string" resultMap="ReportMap">
        SELECT id, reporterId, targetBoard, targetContentId, `type`, `status`, `description`
        FROM report
        WHERE `status` = 'PENDING'
        AND `targetBoard` = #{board}
        ORDER BY id DESC
    </select>

</mapper>
