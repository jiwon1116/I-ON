<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Free">
    <select id="allFreeList" resultType="free">
        SELECT * FROM free_post ORDER BY id DESC
    </select>

    <insert id="write" parameterType="free" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO free_post (title, nickname, content, userId)
        VALUES (#{title}, #{nickname}, #{content}, #{userId})
    </insert>

    <select id="findById" parameterType="long" resultType="free">
        SELECT * FROM free_post WHERE id = #{id}
    </select>

    <delete id="delete" parameterType="long">
        DELETE FROM free_post WHERE id = #{id}
    </delete>

    <update id="update" parameterType="free">
        UPDATE free_post SET title = #{title}, content = #{content} WHERE id = #{id}
    </update>

    <select id="pagingList" parameterType="java.util.HashMap" resultType="free">
        SELECT * FROM free_post ORDER BY id DESC LIMIT #{start}, #{limit}
    </select>

    <select id="boardCount" resultType="int">
        SELECT COUNT(id) FROM free_post
    </select>

    <update id="updateViewCount" keyProperty="int">
        UPDATE free_post SET view_count = view_count + 1
        WHERE id = #{id}
    </update>

    <insert id="saveFile" parameterType="freeFile">
        INSERT INTO free_file (board_id, originalFileName, storedFileName)
        VALUES (#{board_id}, #{originalFileName}, #{storedFileName})
    </insert>

    <select id="findFileById" parameterType="long" resultType="freeFile">
        SELECT * FROM free_file WHERE board_id = #{id}
    </select>

    <delete id="deleteFileById" parameterType="long">
        DELETE FROM free_file WHERE board_id = #{id}
    </delete>

    <update id="increaseLikeCount" parameterType="long">
        UPDATE free_post SET like_count = like_count + 1 WHERE id = #{postId}
    </update>

    <update id="decreaseLikeCount" parameterType="long">
        UPDATE free_post SET like_count = like_count - 1 WHERE id = #{postId} AND like_count > 0
    </update>

    <select id="searchPagingList" parameterType="map" resultType="free">
        SELECT *
        FROM free_post
        WHERE title LIKE CONCAT('%', #{keyword}, '%')
        OR content LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY created_at DESC
        LIMIT #{start}, #{limit}
    </select>

    <select id="searchCount" parameterType="string" resultType="int">
        SELECT COUNT(*)
        FROM free_post
        WHERE title LIKE CONCAT('%', #{keyword}, '%')
        OR content LIKE CONCAT('%', #{keyword}, '%')
    </select>

    <select id="findAllByWriter" parameterType="String" resultType="free">
        SELECT * FROM free_post WHERE userId = #{userId} ORDER BY created_at DESC
    </select>

</mapper>