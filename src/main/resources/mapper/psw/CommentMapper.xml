<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="infoComment">
    <insert id="save" parameterType="infoComment">
        INSERT INTO info_comment (post_id, nickname, content)
        VALUES (#{post_id}, #{nickname}, #{content})
    </insert>

    <select id="findAll" parameterType="long" resultType="infoComment">
        SELECT * FROM info_comment WHERE post_id = #{post_id} ORDER BY id DESC
    </select>

    <delete id="commentDelete" parameterType="infoComment">
        DELETE FROM info_comment
        WHERE id = #{id} AND nickname=#{nickname}
    </delete>

    <select id="infoFindAllComment" resultType="infoComment">
        SELECT * FROM info_comment ORDER BY id DESC
    </select>

</mapper>